/**
 * GlassIsland — v6 Phase 6
 * Bottom nav removed per PRD update. Navigation items moved to the
 * three-dot (⋮) menu in the Header. This file kept as null export so
 * all existing imports compile without changes.
 */
export function GlassIsland() {
  return null;
}
